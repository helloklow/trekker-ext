class Visit {
    constructor(visit) {
        const {id, active, user_id, park_id} = visit
        this.id = id
        this.active = active
        this.park_id = park_id
        this.user_id = user_id
    }

    
}